pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                script {
                    sh "docker build -t spinny-web-automation:v1 -f deployment/Dockerfile ."
                    // below the line to create container name
                    sh "docker create --name spinny3-test-result spinny-web-automation:v1" 
                    // create directory to paste result build from container 
                    sh "mkdir auto1-reports"
                    // copy paste test result to dir in Jenkins
                   // sh "docker cp spinny2-test-result:target/cucumber-html-reports/overview-features.html auto-reports/overview-features.html"
                }
            }
           //post {
                // If Maven was able to run the tests, even if some of the test
                // failed, record the test results and publish HTML file.
             //  success {
                    //publishHTML([
                       // allowMissing: false, 
                       // alwaysLinkToLastBuild: false, 
                       // keepAll: false, 
                       // reportDir: 'auto-reports', 
                       // reportFiles: 'overview-features.html', 
                       // reportName: 'HTML Report', 
                       // reportTitles: ''
                        
   //                 ])
    //            }
      //     }
       }
        stage('clean after build'){
            steps{
                script{
                    println 'Done for test automation'
                    cleanWs()
                }
            }
        }
    }
}
